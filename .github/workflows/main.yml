name: update on push

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest  # Use the latest Ubuntu runner

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: test echo
      #run: echo "$PRIVATE_SSH_KEY" > key.pem && chmod 600 key.pem
      run: echo  "work"
      
    - name: Set up SSH key
      #run: echo "$PRIVATE_SSH_KEY" > key.pem && chmod 600 key.pem
      run: echo  "$PRIVATE_SSH_KEY"

    - name: Run the script on the remote VM
      run: |
        #ssh  -o StrictHostKeyChecking=no -i key.pem ubuntu@51.222.111.185 'bash -s' < /home/ubuntu/koorea_project/launch_backend.sh
        ssh  -o StrictHostKeyChecking=no -i key.pem ubuntu@51.222.111.185 "sh /home/ubuntu/koorea_project/launch_backend.sh"
      env:
        PRIVATE_SSH_KEY: ${{ secrets.PRIVATE_SSH_KEY }}
